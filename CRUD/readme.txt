// To build the container of the Node.js and MongoDB use the command
$ docker build .

// To start the container
$ docker compose up -d 

// Use http://localhost:5200/persons in the URL to open the app 

// To stop the container
$ docker compose down